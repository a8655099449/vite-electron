import { FC, ReactElement } from "react";

type ListBottomLoadingProps = any;
const ListBottomLoading: FC<ListBottomLoadingProps> = (): ReactElement => {
  return <div className="center">加载中...</div>;
};

export default ListBottomLoading;
